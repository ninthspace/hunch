# Coverage: Persistence, labels and calibration

**Number**: 01-06  
**Source epic**: 01-06  
**Status**: pending  

## Coverage

| # | Requirement | Spec Text | Story Criterion | Covered by | Test Approach | Verified |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | FR17 | Publishes migrations for five tables: `hunch_question_sets`, `hunch_classifications`, `hunch_samples`, `hunch_labels` and `hunch_calibration`. | With persistence on, `->record($enquiry)->classify()` writes: - one `hunch_classifications` row, with the subject morph set and `state_hash` equal to sha256 of the rendered state - one `hunch_samples` row per sample - a `hunch_question_sets` row, if one did not already exist | Story 1 | `[integration]` | ✓ |
| 2 | FR17 | `record(Model $subject)` persists a classification against a model. | With persistence on, `->record($enquiry)->classify()` writes: - one `hunch_classifications` row, with the subject morph set and `state_hash` equal to sha256 of the rendered state - one `hunch_samples` row per sample - a `hunch_question_sets` row, if one did not already exist | Story 1 | `[integration]` | ✓ |
| 3 | FR17 | Reasons are stored only when requested. | `hunch_samples.reason` is filled only when `withReasons()` was used; otherwise it is null. | Story 1 | `[integration]` | ✓ |
| 4 | FR17 | Pruning by age uses `Prunable`; `retention_days` must be set when persistence is on. | `model:prune` deletes classification rows older than `retention_days`, together with their samples, and keeps newer rows. | Story 1 | `[integration]` | ✓ |
| 5 | FR17 | `record(Model $subject)` persists a classification against a model. | A failed classification is recorded with status `failed`, the error, and the samples taken. | Story 1 | `[integration]` | ✓ |
| 6 | FR17 | The state is never stored, only `state_hash`. | must NOT — Any column in any Hunch table contains the state text. Checked by embedding a unique marker string in the state and scanning every row. | Story 1 | `[integration]` | ✓ |
| 7 | FR17 | Optional persistence, enabled by `hunch.persistence`. | must NOT — `record()` succeeds when persistence is off; it throws instead. | Story 1 | `[unit]` | ✓ |
| 8 | FR18 | and the history is kept. | must NOT — `model:prune` deletes a `hunch_labels` row, or a classification that has a current label. | Story 1 | `[integration]` | ✓ |
| 9 | FR18 | records human-confirmed answers for any subset of a classification's questions. | `Hunch::label($id, ['intent' => 'refund'], by: $user)` writes one `hunch_labels` row with the labeller morph set, and leaves the classification's other questions unlabelled. | Story 2 | `[integration]` | ✓ |
| 10 | FR18 | A later label for the same classification and question supersedes the earlier one, and the history is kept. | A second label for the same classification and question sets `superseded_at` on the first. Both rows remain, and exactly one is current. | Story 2 | `[integration]` | ✓ |
| 11 | FR18 | `Hunch::label($classificationId, [...], by: $user)` records human-confirmed answers | A label for an unknown question key, or a Choice value that is not a canonical option, throws and writes nothing. | Story 2 | `[integration]` | ✓ |
| 12 | FR18 | `Hunch::label($classificationId, [...], by: $user)` records human-confirmed answers | Labelling a classification ID that does not exist throws. | Story 2 | `[integration]` | ✓ |
| 13 | FR18 | records human-confirmed answers for any subset of a classification's questions. | Only classifications with a current label for the question are counted; unlabelled questions are skipped. | Story 3 | `[integration]` | ✓ |
| 14 | FR18 | A later label for the same classification and question supersedes the earlier one | must NOT — A superseded label is counted by `hunch:calibrate`. | Story 3 | `[integration]` | ✓ |
| 15 | FR18 | A later label for the same classification and question supersedes the earlier one | Relabelling a recorded classification after a calibration run changes that cell's `correct` count on the next `hunch:calibrate` run. | Story 7 | `[integration]` | ✓ |
| 16 | FR19 | it computes `n`, `correct`, `accuracy` and the 95% Wilson score lower bound. | The Wilson score 95% lower bound for 27 correct out of 30 matches a reference value to 1e-6. | Story 3 | `[tdd]` `[unit]` | ✓ |
| 17 | FR19 | it computes the Brier score, the expected calibration error, and a reliability table of mean stated probability against measured accuracy per bucket. | The Brier score for Boolean probabilities [1.0, 0.8, 0.6] against labels [T, F, T] is (0 + 0.64 + 0.16) / 3 to 1e-9. For Choice, it is the multi-class Brier score over the option probability vector. | Story 3 | `[tdd]` `[unit]` | ✓ |
| 18 | FR19 | it computes the Brier score, the expected calibration error, and a reliability table of mean stated probability against measured accuracy per bucket. | The expected calibration error for a hand-built set across 3 buckets equals the count-weighted mean of \|mean stated probability − accuracy\| per bucket, to 1e-9. | Story 3 | `[tdd]` `[unit]` | ✓ |
| 19 | FR19 | (question set hash, driver, provider, model, sampling rule, question, answer, bucket), it computes `n`, `correct`, `accuracy` and the 95% Wilson score lower bound. | Against a seeded SQLite fixture of 40 labelled classifications across 2 drivers, `hunch:calibrate` writes one `hunch_calibration` row per cell (set hash, driver, provider, model, rule, question, answer, bucket). Each row's n, correct, accuracy and lower bound match the fixture's expected values. A classification is correct for a question when its winning answer equals the current label. The winning answer is the Choice's `choice`, the Boolean's value at probability ≥ 0.5, or the Score's modal level. | Story 3 | `[integration]` | ✓ |
| 20 | FR19 | `hunch:calibrate` is a schedulable command. | Running `hunch:calibrate` twice produces identical rows: it is idempotent and replaces previous values. | Story 3 | `[integration]` | ✓ |
| 21 | FR19 | (question set hash, driver, provider, model, sampling rule, question, answer, bucket), it computes `n`, `correct`, `accuracy` and the 95% Wilson score lower bound. | must NOT — Classifications from different drivers or models are merged into one calibration cell. | Story 3 | `[integration]` | ✓ |
| 22 | FR20 | Calibration cells are always kept separate per driver. | must NOT — Classifications from different drivers or models are merged into one calibration cell. | Story 3 | `[integration]` | ✓ |
| 23 | FR20 | Buckets are configurable. | With `hunch.buckets` = `[1.0, 0.9, 0.5]`, a probability of 0.95 falls in `[0.9, 1.0)` and 0.4 falls in `[0, 0.5)`. | Story 4 | `[unit]` | ✓ |
| 24 | FR20 | `$answer->calibration()` returns `{n, accuracy, lowerBound}` for the answer's cell once `n` reaches `calibration.min_n` (default 30) | With a calibration row where n = 30 for the answer's cell, `$answer->calibration()` returns `{n: 30, accuracy, lowerBound}` equal to that row. | Story 4 | `[integration]` | ✓ |
| 25 | FR20 | and `null` before that. | With n = 29 for the cell (below `calibration.min_n` = 30), `calibration()` returns null. | Story 4 | `[integration]` | ✓ |
| 26 | FR20 | Calibration cells are always kept separate per driver. | must NOT — `calibration()` on a sampling-driver answer returns a row computed for the TypeSafe driver, or for a different model or sampling rule. | Story 4 | `[integration]` | ✓ |
| 27 | FR20 | Buckets are configurable. | A bucket configuration that is not strictly descending, or has an edge outside (0, 1], throws a configuration exception. | Story 4 | `[unit]` | ✓ |
| 28 | FR20 | `$answer->calibration()` returns `{n, accuracy, lowerBound}` for the answer's cell once `n` reaches `calibration.min_n` (default 30) | 30 classifications recorded with `record()`, labelled with `Hunch::label()` and calibrated by `hunch:calibrate` into one cell make a later answer in that cell return the computed row from `calibration()`. | Story 7 | `[integration]` | ✓ |
| 29 | FR23 | `redactUsing(callable)` transforms the state once per classification, before the first model call. | must NOT — Any prompt, event or persisted row contains the unredacted text when a redactor is set. | Story 1 | `[integration]` | ✓ |
| 30 | FR24 | Events are dispatched: `Classifying`, `SampleTaken`, `SampleInvalid`, `Classified`, `ClassificationFailed`, `Labelled` and `CalibrationComputed`. | `Hunch::label()` dispatches `Labelled`, and `hunch:calibrate` dispatches `CalibrationComputed` once per run. | Story 3 | `[integration]` | ✓ |
| 31 | FR26 | re-classifies a question set's labelled items with each driver, using the recorded labels as ground truth. It reports accuracy, Brier score, expected calibration error, mean samples and token usage (input, cached input, output) per driver. | Against a seeded SQLite set of 20 labelled classifications, `hunch:eval {hash} --driver=sampling:fake:m1 --driver=sampling:fake:m2` (with faked agents) re-classifies all 20 with each driver and prints one row per driver: accuracy, Brier, ECE, mean samples, and input, cached-input and output tokens. | Story 5 | `[feature]` | ✓ |
| 32 | FR26 | It reports accuracy, Brier score, expected calibration error | Accuracy, Brier and ECE are computed by the same functions `hunch:calibrate` uses, and match the fixture's expected values. | Story 5 | `[integration]` | ✓ |
| 33 | FR26 | re-classifies a question set's labelled items with each driver | A question set hash with no labelled items exits non-zero with a clear message and makes no model calls. | Story 5 | `[feature]` | ✓ |
| 34 | FR26 | `hunch:eval {set} --driver=... [--driver=...]` | A `--driver` value that does not parse as `driver:provider:model` exits non-zero before any model call. | Story 5 | `[unit]` | ✓ |
| 35 | FR26 | using the recorded labels as ground truth. | must NOT — An eval run writes classification or calibration rows that `hunch:calibrate` would count alongside production classifications. | Story 5 | `[integration]` | ✓ |
| 36 | FR27 | `withSelfReport()` asks for a stated confidence band per question. | With `withSelfReport()`, the schema requires a `band` per question from a fixed set of bands defined in the instructions, and each sample's bands are recorded (in `hunch_samples.band` when persisted). | Story 6 | `[integration]` | ✓ |
| 37 | FR27 | `hunch:calibrate` computes the same measures for the stated bands, so the application can see whether agreement or self-report predicts accuracy better. | Against a seeded fixture with bands recorded, `hunch:calibrate` computes accuracy, Brier score and ECE per stated band alongside the agreement-based measures, over the same classifications. | Story 6 | `[integration]` | ✓ |
| 38 | FR27 | The band is recorded for comparison only. | must NOT — A self-reported band changes any vote, probability, bucket or answer. | Story 6 | `[unit]` | ✓ |
| 39 | FR27 | `withSelfReport()` asks for a stated confidence band per question. | Without `withSelfReport()`, the schema has no `band` field. | Story 6 | `[unit]` | ✓ |
| 40 | NFR2 | Only `state_hash` is stored. | must NOT — Any column in any Hunch table contains the state text. Checked by embedding a unique marker string in the state and scanning every row. | Story 1 | `[integration]` | ✓ |
| 41 | NFR2 | Hunch never writes the content of the state to the database, a log or a cache. | With persistence on, reasons on, and a log spy and cache spy attached, a classification whose state contains a unique marker writes that marker to no database row, log entry or cache entry. | Story 1 | `[integration]` | ✓ |
| 42 | NFR2 | Hunch never writes the content of the state to the database, a log or a cache. | For a failed classification whose state contains a unique marker, the marker appears in neither the `ClassificationFailed` message nor the recorded `error` column. | Story 1 | `[integration]` | ✓ |
| 43 | NFR3 | with persistence off, a classification makes no database queries and needs no migrations. | With persistence off, `calibration()` returns null and makes no database query. | Story 4 | `[integration]` | ✓ |
| 44 | ENV6 | the persistence, label and calibration tests run against SQLite in memory | `phpunit.xml` sets `DB_CONNECTION=sqlite` and `DB_DATABASE=:memory:`, and a persistence test migrates and passes. | Story 1 | `[integration]` | ✓ |
