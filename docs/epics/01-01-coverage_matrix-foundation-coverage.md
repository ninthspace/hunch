# Coverage: Package foundation and CI

**Number**: 01-01  
**Source epic**: 01-01  
**Status**: pending  

## Coverage

| # | Requirement | Spec Text | Story Criterion | Covered by | Test Approach | Verified |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | NFR4 | the package's own test suite passes with no network access. | `Http::preventStrayRequests()` is enabled globally in the test bootstrap. | Story 1 | `[unit]` | ✓ |
| 2 | NFR4 | the package's own test suite passes with no network access. | `composer test:offline` runs `vendor/bin/pest` with no provider API keys set and outbound network blocked, and passes. | Story 2 | `[integration]` | ✓ |
| 3 | ENV1 | ~~the host runs PHP 8.3 or later.~~ | ~~`composer.json` requires `php` `^8.3`.~~ | Story 1 | `[unit]` | **Withdrawn 2026-09-19T18:10:45.830Z** — ENV1 amended (pivot 2026-09-19): "the host runs PHP 8.3 or later." removed; Hunch is a package with no host. Rebound to the composer.json constraint clause. |
| 4 | ENV1 | Its composer.json constrains `php` to `^8.3` | `composer.json` requires `php` `^8.3`. | Story 1 | `[unit]` | ✓ |
| 5 | ENV1 | ~~the host runs PHP 8.3 or later.~~ | ~~On the production host, `php -v` reports 8.3 or later.~~ | Story 1 | `[target]` | **Withdrawn 2026-09-19T17:50:00Z** — The criterion this bound was superseded: Hunch is a package with no production host. Composer enforces `php` `^8.3` in any installing app, and criterion 3's composer.json test covers it. |
| 6 | ENV2 | ~~The package's composer constraint for `illuminate/*` is `^12.0\|^13.0`.~~ | ~~`composer.json` constrains every `illuminate/*` requirement to `^13.0`.~~ | Story 1 | `[unit]` | **Withdrawn 2026-09-19T15:47:46.865Z** — ENV2 amended to Laravel 13 only; the quoted `^12.0\|^13.0` constraint no longer appears in the requirement. |
| 7 | ENV2 | The package's composer constraint for `illuminate/*` is `^13.0`. | `composer.json` constrains every `illuminate/*` requirement to `^13.0`. | Story 1 | `[unit]` | ✓ |
| 8 | ENV2 | ~~the host application runs Laravel 12.x or 13.x.~~ | ~~In the host application, `composer show laravel/framework` reports 13.x, and installing Hunch reports no dependency conflict.~~ | Story 1 | `[target]` | **Withdrawn 2026-09-19T15:47:46.898Z** — ENV2 amended to Laravel 13 only; "12.x or 13.x" no longer appears in the requirement. |
| 9 | ENV2 | ~~the host application runs Laravel 13.x.~~ | ~~In the host application, `composer show laravel/framework` reports 13.x, and installing Hunch reports no dependency conflict.~~ | Story 1 | `[target]` | **Withdrawn 2026-09-19T17:50:00Z** — The criterion this bound was superseded: Hunch is a package with no production host. Composer refuses to resolve Hunch against a framework outside `illuminate/*` `^13.0`, and criterion 5's composer.json test covers it. |
| 10 | ENV3 | ~~the host has a `laravel/ai` version~~ | ~~`composer.json` requires `laravel/ai`.~~ | Story 1 | `[unit]` | **Withdrawn 2026-09-19T18:10:45.846Z** — ENV3 amended (pivot 2026-09-19): "the host has" removed; Hunch is a package with no host. Rebound to "Hunch requires a `laravel/ai` version". |
| 11 | ENV3 | Hunch requires a `laravel/ai` version | `composer.json` requires `laravel/ai`. | Story 1 | `[unit]` | ✓ |
| 12 | ENVX3 | Its composer.json declares no additional `ext-*` requirements. | Hunch's `composer.json` declares no `ext-*` requirement beyond those Laravel requires. | Story 1 | `[unit]` | ✓ |
| 13 | ENVX3 | ~~Hunch must not require any PHP extension beyond those Laravel itself requires.~~ | ~~`composer check-platform-reqs` passes on the production host with Hunch installed.~~ | Story 1 | `[target]` | **Withdrawn 2026-09-19T17:50:00Z** — The criterion this bound was superseded: Hunch is a package with no production host. It declares no `ext-*` requirement of its own, so it adds nothing to an installing app's platform check; criterion 8's composer.json test covers it. |
| 14 | ENVX3 | Hunch must not require any PHP extension beyond those Laravel itself requires. | A test confirms that no function called and no class imported under `src/` comes from a PHP extension outside PHP's core and those `laravel/framework` requires. | Story 1 | `[unit]` | ✓ |
| 15 | ENV4 | `orchestra/testbench` is a dev dependency and boots Laravel for the package tests. | `composer.json` `require-dev` includes `orchestra/testbench`, and the base `TestCase` extends Testbench's `TestCase`. | Story 1 | `[unit]` | ✓ |
| 16 | ENV5 | Pest 4 is installed as a dev dependency, and `vendor/bin/pest` runs the suite, including architecture tests. | `vendor/bin/pest --version` reports 4.x, and `tests/ArchTest.php` exists and runs. | Story 1 | `[integration]` | ✓ |
| 17 | ENV6 | the persistence, label and calibration tests run against SQLite in memory | `phpunit.xml` sets `DB_CONNECTION=sqlite` and `DB_DATABASE=:memory:`. | Story 1 | `[integration]` | ✓ |
| 18 | ENV7 | ~~a GitHub Actions workflow runs the suite on every push and pull request. The matrix covers PHP 8.3, 8.4 and 8.5 × Laravel 12 and 13, each with prefer-lowest and prefer-stable.~~ | ~~`composer test` runs `vendor/bin/pest` against Laravel 13, and passes.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:47:46.938Z** — ENV7 amended: no GitHub Actions and no matrix; the suite runs locally via `composer test` against Laravel 13. |
| 19 | ENV7 | `composer test` runs the suite locally against Laravel 13. | `composer test` runs `vendor/bin/pest` against Laravel 13, and passes. | Story 2 | `[integration]` | ✓ |
| 20 | ENV8 | ~~CI runs PHPStan with Larastan at level max, and `pint --test`, and fails on any error.~~ | ~~`composer check` runs `phpstan analyse` at level max with Larastan, and `pint --test`. A deliberate violation of either makes it exit non-zero.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:48:19.640Z** — ENV8 amended: PHPStan and Pint run locally via `composer check`, not in CI. |
| 21 | ENV8 | a local composer script (`composer check`) runs PHPStan with Larastan at level max and `pint --test`, and exits non-zero on any error. | `composer check` runs `phpstan analyse` at level max with Larastan, and `pint --test`. A deliberate violation of either makes it exit non-zero. | Story 2 | `[integration]` | ✓ |
| 22 | ENVX5 | the default test suite must not require provider API keys or network access. | `composer test` passes with no provider API keys set in the environment. | Story 2 | `[integration]` | ✓ |
| 23 | ENVX5 | ~~Only the nightly compatibility workflow uses them.~~ | ~~must NOT — Any test in the default suite is skipped because an API key or the network is missing. Such tests live only in the nightly suite.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:50:17.708Z** — ENVX5 amended: the manual compatibility command, not a nightly workflow, is the only user of provider keys. |
| 24 | ENVX5 | Only the manual compatibility command uses them. | must NOT — Any test in the default suite is skipped because an API key or the network is missing. Such tests live only in the nightly suite. | Story 2 | `[integration]` | ✓ |
| 25 | ENV10 | ~~the default CI test job can run the suite with outbound network access disabled.~~ | ~~`composer test:offline` runs Pest with outbound network denied at the OS level (on macOS, `sandbox-exec` with a `deny network-outbound` profile), and a deliberate outbound request in a probe test fails there.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:48:19.682Z** — ENV10 amended: the offline run is local, not a CI test job. |
| 26 | ENV10 | the default test suite can run locally with outbound network access disabled. | `composer test:offline` runs Pest with outbound network denied at the OS level (on macOS, `sandbox-exec` with a `deny network-outbound` profile), and a deliberate outbound request in a probe test fails there. | Story 2 | `[integration]` | ✓ |
| 27 | ENV10 | ~~the default CI test job can run the suite with outbound network access disabled.~~ | ~~`composer test:offline` runs `vendor/bin/pest` with no provider API keys set and outbound network blocked, and passes.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:48:19.708Z** — ENV10 amended: the offline run is local, not a CI test job. |
| 28 | ENV10 | the default test suite can run locally with outbound network access disabled. | `composer test:offline` runs `vendor/bin/pest` with no provider API keys set and outbound network blocked, and passes. | Story 2 | `[integration]` | ✓ |
| 29 | NFR8 | the Composer name is `ninthspace/hunch` and the root namespace is `Ninthspace\Hunch\` | An architecture test confirms every class under `src/` lives in the `Ninthspace\Hunch\` namespace, and `composer.json` declares the name `ninthspace/hunch`. | Story 1 | `[unit]` |  |
| 30 | NFR8 | ~~Nothing is published (no Packagist submission, no public repository, no tagged release) until ownership of the code is confirmed.~~ | ~~must NOT — The package is submitted to Packagist, the repository is made public, or a release is tagged before ownership of the code has been confirmed in writing.~~ | Story 1 | `[manual]` | **Withdrawn 2026-09-20T09:57:59.273Z** — The clause it quoted was removed from NFR8 on 2026-09-20. The release gate was a mistake in the original spec: authorship and origin of the code were never in question, so there was nothing for publication to wait on. |
| 31 | ENVX6 | the repository must not contain GitHub Actions workflows. | must NOT — The repository contains a `.github/workflows` directory or any file that triggers a GitHub Actions run. | Story 2 | `[unit]` | ✓ |
