# Coverage: Sampling driver and classify pipeline

**Number**: 01-04  
**Source epic**: 01-04  
**Status**: pending  

## Coverage

| # | Requirement | Spec Text | Story Criterion | Covered by | Test Approach | Verified |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | FR1 | `classify()`, which returns a `Result`. | `Hunch::of([...])->questions([...])->using('anthropic', 'm')->sampling(3)->classify()` returns a `Result` whose answers are keyed by the question keys. | Story 2 | `[feature]` | ✓ |
| 2 | FR1 | `using(provider, ?model)` | `classify()` with no `using()` and no `hunch.provider` configured throws a configuration exception before any model call. | Story 2 | `[unit]` | ✓ |
| 3 | FR1 | anything else the sampling driver. | must NOT — `using('openai', ...)` routes to a driver other than sampling, or calls any provider other than openai. | Story 2 | `[integration]` | ✓ |
| 4 | FR1 | `question()`/`questions()` | `classify()` with an empty question set throws before any model call. | Story 2 | `[unit]` | ✓ |
| 5 | FR3 | Provider, model and timeout are passed to `prompt()`. | With `ClassifierAgent::fake()` returning valid structured output, `classify()` with fixed 3 sampling prompts the agent 3 times, each with the provider, model and timeout given to the builder. | Story 2 | `[integration]` |  |
| 6 | FR3 | ~~Temperature is 1.0, max tokens are sized from the schema, instructions are cached, and TopP is not set.~~ | ~~Reflection on `ClassifierAgent` shows `Temperature(1.0)`, `MaxTokens` and `CacheInstructions` attributes, and no `TopP`.~~ | Story 2 | `[unit]` | **Withdrawn 2026-09-20T08:33:02.510Z** — FR3's first bullet was reworded on 2026-09-20: max tokens are no longer sized from the schema, so the quoted sentence is no longer a clause the requirement contains. The criterion it named is still true and is rebound to the new sentence. |
| 7 | FR3 | Temperature is 1.0, max tokens are set well above any valid answer so a good sample is never truncated, instructions are cached, and TopP is not set. | Reflection on `ClassifierAgent` shows `Temperature(1.0)`, `MaxTokens` and `CacheInstructions` attributes, and no `TopP`. | Story 2 | `[unit]` | ✓ |
| 8 | FR3 | ~~max tokens are sized from the schema~~ | ~~The computed max tokens grows with the number of questions, and grows further when reasons are enabled.~~ | Story 2 | `[unit]` | **Withdrawn 2026-09-20T08:33:02.523Z** — FR3 no longer says max tokens are sized from the schema, and the criterion this bound — that the computed ceiling grows with the question count — was superseded with the formula it described. |
| 9 | FR3 | The agent class can be replaced through configuration. | Setting `hunch.agent` to a subclass of `ClassifierAgent` makes the sampling driver prompt that subclass. | Story 2 | `[integration]` |  |
| 10 | FR3 | The agent is not conversational. | must NOT — `ClassifierAgent` implements `Conversational`, or SDK conversation storage is written during a classification. | Story 2 | `[unit]` |  |
| 11 | FR3 | Provider, model and timeout are passed to `prompt()`. | Builder calls override config: with `hunch.timeout` = 20, `->timeout(5)` passes 5 to `prompt()`. | Story 2 | `[integration]` |  |
| 12 | FR5 | A classification can therefore be reproduced. | `Result->meta` lists the seed for every sample taken. | Story 3 | `[integration]` | ✓ |
| 13 | FR6 | `sampling(N)` takes N samples | `sampling(5)` prompts the agent exactly 5 times, even when the first 3 samples are unanimous. | Story 2 | `[integration]` | ✓ |
| 14 | FR6 | `sampling(N)` takes N samples | `sampling(0)` or a negative number throws before any model call. | Story 2 | `[unit]` | ✓ |
| 15 | FR6 | one after another. | must NOT — A later sample starts before the previous `prompt()` call has returned. | Story 2 | `[integration]` | ✓ |
| 16 | FR10 | Answer objects with the same shape as the SDK's answers, plus Hunch's additions. | A contract test runs the same scripted input through the sampling driver and the fake driver, and asserts that each answer class exposes the same public properties and methods from both. | Story 6 | `[integration]` | ✓ |
| 17 | FR10 | `confidence` is the top vote share, with the same meaning for every driver. | For the same votes a,a,a,b, `ChoiceAnswer::confidence` is 0.75 from both the sampling driver and the fake driver. | Story 6 | `[integration]` | ✓ |
| 18 | FR11 | `Result` implements `ArrayAccess` by question key (read-only) | `$result['intent']` returns the same object as `$result->answers['intent']`; an unknown key throws. | Story 3 | `[unit]` | ✓ |
| 19 | FR11 | `samples` (requested, valid, invalid) | After 5 samples, 1 of them invalid, `samples` reports requested 5, valid 4, invalid 1. | Story 3 | `[integration]` | ✓ |
| 20 | FR11 | `usage`, summed across samples and including cache reads and writes | `usage` equals the sum over all samples of input, cached-input (read and write) and output tokens, as reported by the faked agent responses. | Story 3 | `[integration]` | ✓ |
| 21 | FR11 | `meta`: driver, provider, model requested and model reported, sampling rule, question set hash and version, seeds, request IDs | `meta` contains driver, provider, model requested, model reported, sampling rule, question set hash, version name, seeds and request IDs, each filled from the faked responses. | Story 3 | `[integration]` | ✓ |
| 22 | FR11 | `id`: the classification's ULID, always present, and the recorded row's key when persisted | `id` is the classification's ULID, whether or not the classification is recorded. | Story 3 | `[unit]` | ✓ |
| 23 | FR11 | `Result` implements `ArrayAccess` by question key (read-only) | must NOT — `$result['intent'] = ...` or `unset($result['intent'])` succeeds. | Story 3 | `[unit]` | ✓ |
| 24 | FR12 | Samples that fail schema validation are recorded and excluded from counting. | A sample whose output fails schema validation (a missing field, or an enum value that is not a canonical key) is counted as invalid and excluded from every vote. | Story 4 | `[integration]` | ✓ |
| 25 | FR12 | `classify()` throws `ClassificationFailed` when fewer than `min_valid_samples` valid samples are obtained (default 3) | With `max_resamples` = 0, 5 samples, 3 of them invalid, and `min_valid_samples` = 3, `classify()` throws `ClassificationFailed` carrying the 2 valid and 3 invalid samples, and the usage summed across all 5. | Story 4 | `[integration]` | ✓ |
| 26 | FR12 | The exception carries any partial samples and their usage. | With `max_resamples` = 0, 5 samples, 3 of them invalid, and `min_valid_samples` = 3, `classify()` throws `ClassificationFailed` carrying the 2 valid and 3 invalid samples, and the usage summed across all 5. | Story 4 | `[integration]` | ✓ |
| 27 | FR12 | or when the provider still fails after retries. | A provider error that persists after the configured retries throws `ClassificationFailed` carrying the samples taken so far. | Story 4 | `[integration]` | ✓ |
| 28 | FR12 | The exception carries any partial samples and their usage. | A provider error that persists after the configured retries throws `ClassificationFailed` carrying the samples taken so far. | Story 4 | `[integration]` | ✓ |
| 29 | FR12 | `classify()` throws `ClassificationFailed` when fewer than `min_valid_samples` valid samples are obtained (default 3) | must NOT — `classify()` returns a `Result` when fewer than `min_valid_samples` valid samples were obtained. | Story 4 | `[integration]` | ✓ |
| 30 | FR12 | Hunch never substitutes a default answer. | must NOT — Any answer carries a default value, rather than one computed from counted samples, when a failure occurred. | Story 4 | `[unit]` | ✓ |
| 31 | FR12 | If `min_valid_samples` exceeds the fewest samples the rule can take (N for fixed, `min` for adaptive), a configuration exception is thrown before any model call. | `sampling(2)` or `adaptive: [2, 5]` with `min_valid_samples` = 3 throws a configuration exception before any model call. `sampling(3)` with the same setting proceeds. | Story 4 | `[unit]` | ✓ |
| 32 | FR13 | Rate-limit and overload errors are retried with backoff against the same provider, up to `retries` times (default 2). | A faked rate-limit error on the first 2 attempts, then success, with `retries` = 2, gives a successful sample against the same provider after 3 attempts, with backoff sleeps between them (asserted via `Sleep::fake()`). | Story 5 | `[integration]` | ✓ |
| 33 | FR13 | Rate-limit and overload errors are retried with backoff against the same provider, up to `retries` times (default 2). | A rate-limit error on 3 attempts with `retries` = 2 throws `ClassificationFailed`. | Story 5 | `[integration]` | ✓ |
| 34 | FR13 | Rate-limit and overload errors are retried with backoff against the same provider, up to `retries` times (default 2). | A non-retryable provider error (such as authentication) is not retried. | Story 5 | `[integration]` | ✓ |
| 35 | FR13 | The SDK's cross-provider failover is disabled unless `failover` is set to true. | must NOT — With `failover` false, a sample is sent to any provider other than the configured one, even after retries are exhausted. | Story 5 | `[integration]` | ✓ |
| 36 | FR13 | The SDK's cross-provider failover is disabled unless `failover` is set to true. | With `failover` true, the SDK's failover configuration is passed through to the prompt call. | Story 5 | `[integration]` | ✓ |
| 37 | FR15 | `Hunch::fake([...])` returns answers directly, for testing application logic. | `Hunch::fake(['intent' => ['refund' => 0.8, 'other' => 0.2], 'reply_today' => 0.6])` makes `classify()` return those probabilities, with no agent call. | Story 6 | `[feature]` | ✓ |
| 38 | FR15 | `Hunch::fake()->samples([...])` runs the real aggregation over scripted samples. | `Hunch::fake()->samples([...])` with 3 scripted samples (refund, refund, other) returns answers computed by the real aggregator: `intent` refund with probability 2/3. | Story 6 | `[feature]` | ✓ |
| 39 | FR15 | `assertClassified(callable)`, `assertSampledTimes(n)` and `preventStrayClassifications()`. | `assertClassified(fn)` passes when a matching classification happened, and fails with a readable message when none did. | Story 6 | `[feature]` | ✓ |
| 40 | FR15 | `assertClassified(callable)`, `assertSampledTimes(n)` and `preventStrayClassifications()`. | `assertSampledTimes(3)` passes after 3 scripted samples and fails after 2. | Story 6 | `[feature]` | ✓ |
| 41 | FR15 | `assertClassified(callable)`, `assertSampledTimes(n)` and `preventStrayClassifications()`. | Under `preventStrayClassifications()`, a classification with no matching fake throws. | Story 6 | `[feature]` | ✓ |
| 42 | FR15 | Seeds are fixed under the fake, so shuffles are deterministic. | Two runs of the same classification under the fake produce identical user messages (seeds are fixed). | Story 6 | `[integration]` | ✓ |
| 43 | FR16 | A publishable `config/hunch.php` | `vendor:publish --tag=hunch-config` writes `config/hunch.php` containing every key listed in FR16, each with its documented default. | Story 1 | `[integration]` | ✓ |
| 44 | FR16 | `fixed:5` (`fixed:N` or `adaptive:MIN-MAX`) | `hunch.sampling` = `adaptive:3-7` parses into an adaptive rule [3, 7]. A malformed value (such as `adaptive:7-3` or `foo:5`) throws a configuration exception when first used. | Story 1 | `[unit]` | ✓ |
| 45 | FR16 | none; required when persistence is on | `hunch.persistence` true with `hunch.retention_days` null throws a configuration exception at boot. | Story 1 | `[integration]` | ✓ |
| 46 | FR16 | none; must be set if `using()` isn't called | `classify()` with no `using()` and no `hunch.provider` configured throws a configuration exception before any model call. | Story 2 | `[unit]` | ✓ |
| 47 | FR16 | `fixed:5` (`fixed:N` or `adaptive:MIN-MAX`) | With no `sampling()` call, the `hunch.sampling` config applies (default `fixed:5`). | Story 2 | `[integration]` | ✓ |
| 48 | FR17 | `retention_days` must be set when persistence is on. | `hunch.persistence` true with `hunch.retention_days` null throws a configuration exception at boot. | Story 1 | `[integration]` | ✓ |
| 49 | NFR1 | Only output that validates against the schema is counted. | A faked sample that returns an extra field, or free text instead of the schema, is counted as invalid. | Story 4 | `[integration]` | ✓ |
| 50 | NFR1 | Only output that validates against the schema is counted. | must NOT — A model response with a value outside the schema's enums contributes a vote. | Story 4 | `[integration]` | ✓ |
| 51 | NFR3 | with persistence off, a classification makes no database queries and needs no migrations. | must NOT — The service provider loads Hunch migrations when persistence is off. | Story 1 | `[integration]` | ✓ |
| 52 | NFR3 | with persistence off, a classification makes no database queries and needs no migrations. | With persistence off and `DB::listen` attached, a full classification, including calls to each answer's `calibration()`, runs zero queries. | Story 7 | `[integration]` | ✓ |
| 53 | NFR3 | with persistence off, a classification makes no database queries and needs no migrations. | With persistence off and the default database connection pointing to a non-existent driver, `classify()` succeeds. | Story 7 | `[integration]` | ✓ |
| 54 | NFR4 | The sampling driver is exercised end to end through the SDK's `ClassifierAgent::fake()`. | At least one test drives `SamplingDriver` end to end through `ClassifierAgent::fake()`: builder, then prompts, then aggregation, then `Result`. | Story 7 | `[feature]` | ✓ |
| 55 | NFR5 | for a given question set hash, the instructions are byte-identical across samples and classifications, so providers can cache them. | Two classifications with the same question set hash and different states send byte-identical instructions, compared by sha256 of the captured prompts. | Story 7 | `[integration]` | ✓ |
| 56 | ENV3 | ~~the host has a `laravel/ai` version providing `Agent`, `HasStructuredOutput`, `prompt()` with provider, model and timeout arguments, and `Agent::fake()`.~~ | ~~`composer.json` requires `laravel/ai`, and in the package suite a `classify()` through the SDK's fake returns a `Result`.~~ | Story 8 | `[integration]` `[target]` | **Withdrawn 2026-09-19T18:10:45.858Z** — ENV3 amended (pivot 2026-09-19): "the host has" removed; Hunch is a package with no host. Rebound to the reworded full sentence. |
| 57 | ENV3 | ~~Hunch requires a `laravel/ai` version providing `Agent`, `HasStructuredOutput`, `prompt()` with provider, model and timeout arguments, and `Agent::fake()`.~~ | ~~`composer.json` requires `laravel/ai`, and in the package suite a `classify()` through the SDK's fake returns a `Result`.~~ | Story 8 | `[integration]` `[target]` | **Withdrawn 2026-09-19T19:15:00Z** — The criterion this bound was superseded: Replaced by an identical criterion tagged `integration` only. The original still carried the `target` tag left from the production-host framing, and dpm has no tool to remove an approach tag. |
| 58 | ENV3 | Hunch requires a `laravel/ai` version providing `Agent`, `HasStructuredOutput`, `prompt()` with provider, model and timeout arguments, and `Agent::fake()`. | `composer.json` requires `laravel/ai`, and in the package suite a `classify()` through the SDK's fake returns a `Result`. | Story 8 | `[integration]` | ✓ |
| 59 | ENVX1 | ~~Hunch must not require a queue worker.~~ | ~~On the host, with no queue worker running, a `classify()` called from a web request or tinker returns a `Result`.~~ | Story 8 | `[target]` | **Withdrawn 2026-09-19T18:15:00Z** — The criterion this bound was superseded: Pivot on spec 01: Hunch is a package with no host, and the spec no longer refers to queues. Criterion 5 covers synchronous `classify()` in the package suite. |
| 60 | ENVX1 | `classify()` runs synchronously wherever it is called. | In the package suite, `classify()` returns a `Result` directly to its caller. | Story 8 | `[integration]` | ✓ |
| 61 | ENVX2 | Hunch must not require a database connection unless `hunch.persistence` is true. | With persistence off and `DB::listen` attached, a full classification, including calls to each answer's `calibration()`, runs zero queries. | Story 7 | `[integration]` | ✓ |
| 62 | ENVX2 | Hunch must not require a database connection unless `hunch.persistence` is true. | With persistence off and the default database connection pointing to a non-existent driver, `classify()` succeeds. | Story 7 | `[integration]` | ✓ |
| 63 | ENVX2 | ~~Hunch must not require a database connection unless `hunch.persistence` is true.~~ | ~~In the package suite, with persistence off and no database connection configured, a `classify()` returns a `Result`.~~ | Story 8 | `[integration]` `[target]` | **Withdrawn 2026-09-19T19:15:00Z** — The criterion this bound was superseded: Replaced by an identical criterion tagged `integration` only. The original still carried the `target` tag left from the production-host framing, and dpm has no tool to remove an approach tag. |
| 64 | ENVX2 | Hunch must not require a database connection unless `hunch.persistence` is true. | In the package suite, with persistence off and no database connection configured, a `classify()` returns a `Result`. | Story 8 | `[integration]` | ✓ |
| 65 | ENVX4 | ~~Hunch must not require network access to any host other than the configured provider's endpoint, so a local model on its own is enough.~~ | ~~On a host whose outbound network is limited to the local model endpoint, a `classify()` against Ollama succeeds.~~ | Story 8 | `[target]` | **Withdrawn 2026-09-19T18:15:00Z** — The criterion this bound was superseded: Pivot on spec 01: Hunch is a package with no host, and Ollama is not part of the plan. ENVX4 is covered by criterion 6's architecture test (no `Http` facade or raw HTTP client under `src/`). |
| 66 | ENVX4 | Hunch must not require network access to any host other than the configured provider's endpoint, so a local model on its own is enough. | An architecture test shows no class under `src/` uses the `Http` facade or a raw HTTP client, so every model call goes through the SDK agent. | Story 8 | `[unit]` | ✓ |
