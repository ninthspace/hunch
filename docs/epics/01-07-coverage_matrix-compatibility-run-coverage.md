# Coverage: Manual compatibility run

**Number**: 01-07  
**Source epic**: 01-07  
**Status**: pending  

## Coverage

| # | Requirement | Spec Text | Story Criterion | Covered by | Test Approach | Verified |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | NFR1 | the state is treated as data. | The compatibility run includes an injection fixture whose state instructs the model to answer a fixed option. It records, and reports but does not fail on, each provider's rate of compliance. | Story 1 | `[integration]` | ✓ |
| 2 | NFR5 | for a given question set hash, the instructions are byte-identical across samples and classifications, so providers can cache them. | In the compatibility run against Anthropic, with a question set above the model's minimum cacheable length, the second and later samples report `cacheReadInputTokens` > 0. | Story 1 | `[integration]` | ✓ |
| 3 | NFR6 | ~~a nightly workflow runs a public, synthetic labelled fixture set against Anthropic, OpenAI, Gemini and one local OpenAI-compatible model.~~ | ~~`composer compat` runs the public synthetic fixture set against Anthropic. It fails if Anthropic's samples are schema-invalid more than 10% of the time.~~ | Story 1 | `[integration]` | **Withdrawn 2026-09-19T15:51:00.651Z** — NFR6 amended: a manual local command runs the fixture set against Anthropic only. |
| 4 | NFR6 | a manual, local command (`composer compat`) runs a public, synthetic labelled fixture set against Anthropic. | `composer compat` runs the public synthetic fixture set against Anthropic. It fails if Anthropic's samples are schema-invalid more than 10% of the time. | Story 1 | `[integration]` | ✓ |
| 5 | NFR6 | It asserts schema validity and aggregation | For every provider, aggregation over real samples produces probabilities that sum to 1 and a `Result` with a fully populated `meta`. | Story 1 | `[integration]` | ✓ |
| 6 | NFR6 | a public, synthetic labelled fixture set | The fixture set is committed under `tests/Fixtures/compat/`, contains no real personal data, and every item carries a label. | Story 1 | `[manual]` `[unit]` | ✓ |
| 7 | NFR6 | It asserts schema validity and aggregation | The fixture set includes a Choice question with 40–50 options, and the compatibility report shows its invalid-sample rate and accuracy per provider separately. | Story 1 | `[integration]` | ✓ |
| 8 | NFR6 | ~~publishes accuracy and token usage per provider in the README.~~ | ~~`composer compat` writes accuracy and token usage per provider into the README results table and changes nothing else; it does not commit, push or open a pull request.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:51:47.289Z** — NFR6 amended: results are written into the README table locally rather than published by a workflow. |
| 9 | NFR6 | writes accuracy and token usage per provider into the README's results table. | `composer compat` writes accuracy and token usage per provider into the README results table and changes nothing else; it does not commit, push or open a pull request. | Story 2 | `[integration]` | ✓ |
| 10 | ENV9 | ~~a scheduled nightly GitHub Actions workflow has the Anthropic, OpenAI and Gemini API keys as repository secrets~~ | ~~`composer compat` reads `ANTHROPIC_API_KEY` from the local environment, exits non-zero with a clear message when it is absent, and completes a run when it is present.~~ | Story 1 | `[integration]` | **Withdrawn 2026-09-19T15:51:00.611Z** — ENV9 amended: the Anthropic key comes from the local environment; there is no scheduled GitHub workflow or repository secret. |
| 11 | ENV9 | the developer's local environment provides `ANTHROPIC_API_KEY` for the compatibility command | `composer compat` reads `ANTHROPIC_API_KEY` from the local environment, exits non-zero with a clear message when it is absent, and completes a run when it is present. | Story 1 | `[integration]` | ✓ |
| 12 | ENV9 | ~~It starts a local OpenAI-compatible model (Ollama) inside the job.~~ | ~~`composer compat` reads `ANTHROPIC_API_KEY` from the local environment, exits non-zero with a clear message when it is absent, and completes a run when it is present.~~ | Story 1 | `[integration]` | **Withdrawn 2026-09-19T15:51:00.622Z** — ENV9 amended: Ollama is no longer part of the compatibility run. |
| 13 | ENV9 | No provider key is stored in the repository or in any CI secret store. | must NOT — A provider key is read from anywhere other than the local environment, or written to the repository. | Story 1 | `[integration]` | ✓ |
| 14 | ENVX5 | ~~Only the nightly compatibility workflow uses them.~~ | ~~must NOT — A provider key is read from anywhere other than the local environment, or written to the repository.~~ | Story 1 | `[integration]` | **Withdrawn 2026-09-19T15:51:23.817Z** — Criterion rewritten from a workflow-trigger rejection to a key-source rejection after the removal of GitHub Actions; ENVX5 no longer names a nightly workflow. |
| 15 | ENV11 | ~~the nightly compatibility workflow has permission to open or update a pull request that changes the README.~~ | ~~After `composer compat`, `git diff --stat` shows changes only to `README.md`, confined to the results table.~~ | Story 2 | `[integration]` | **Withdrawn 2026-09-19T15:51:47.319Z** — ENV11 amended: no workflow opens pull requests; the command writes only the README table and committing is left to the developer. |
| 16 | ENV11 | the compatibility command writes only the README's results table; committing it is left to the developer. | After `composer compat`, `git diff --stat` shows changes only to `README.md`, confined to the results table. | Story 2 | `[integration]` | ✓ |
